event_inherited();

smokeTrailPart = global.smokeTrailPart; // localizing globals... Hm
explosionPart = global.explosionPart;

image_blend = make_color_rgb(irandom(32), irandom(32), irandom(42)); // lower range

duration = 120;

depth -= 10;